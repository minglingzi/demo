micronaut:
  application:
    name: demo
datasources:
  default:
    driver-class-name: com.mysql.cj.jdbc.Driver
    db-type: mysql
    dialect: MYSQL
liquibase:
  datasources:
    default:
      change-log: classpath:db/liquibase-changelog.xml
netty:
  default:
    allocator:
      max-order: 3
